version: 2.1

jobs:
  hello-world:
    docker:
      - image: cimg/python:3.10.9
    steps:
      - checkout
      - run: echo "hello world"
      - run: python --version 

workflows:
  my-workflow:
    jobs:
      - hello-world